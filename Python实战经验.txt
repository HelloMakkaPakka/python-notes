写随机选多人的语句时，注意防止重复选人和循环语句的选择

编写爬虫时，先把框架写好，再把网页分析的代码补上

路径报错解决方法：路径中的'\'对后面的字符进行转义
    在路径前添加r即为保留字符原始值的意思
    替换单反斜杆为双反斜杆
    将单反斜杆替换为正斜杆

'str' object is not callable：
    可能的错误：
        在执行 'a()' 之前已存在一个 'a' 的字符串

py转exe后运行闪退可能原因：
    依赖文件和exe不在同一文件夹

